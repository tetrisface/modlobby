{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: IPC to the lobby runtime and opening local paths.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
