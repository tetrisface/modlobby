[package]
name = "tweaks"
description = "BAR tweakdefs/tweakunits: base64url, StyLua formatting, minification, the !bSet command and its size gauge, diffs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
base64 = { workspace = true }
full_moon = { workspace = true }
serde = { workspace = true }
sha2 = { workspace = true }
similar = { workspace = true }
spring-protocol = { workspace = true }
stylua = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
ts-rs = { workspace = true }

[dev-dependencies]
# Only for `examples/header_survival.rs`, which checks the minifier against a
# real `optionsPresets.json` rather than against fixtures we wrote ourselves.
serde_json = { workspace = true }
